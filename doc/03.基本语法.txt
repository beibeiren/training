Java基本语法
	3.1 数据类型和变量
		总述
			编写程序的目的是为了处理数据
			数据用变量来表示
			变量本质上就是计算机一块内存空间
			各种不同的数据类型
		数据类型
			基本数据类型
				数值型
					整数类型：byte，short，int， long
							 8bit  16bit  32bit 64bit
					浮点类型：float，double
							 32bit  64bit
				字符型：char
					   长度：16bit
					   Java使用Unicode编码
				布尔型：boolean
						 1bit	
			引用数据类型
				类：class
				接口：interface
				数组：array
		变量
			变量的声明与使用
				变量数据类型 变量名 = 变量值
				int         age   =  20；
			变量的命名规则
				字母，数字，下划线，美元符号
			字符串类型
				String
			转义字符
				转义符号 '\'
		数据类型转换
			自动类型转换
			强制数据类型转换
	3.2 运算符
		赋值运算符：=
			符合赋值运算符：+=，-=，*=，/=，%=
		算术运算符：+，-，*，/，%，++，--
		关系运算符：>, >=, ==, <=，<, !=
		逻辑运算符：&&, ||
		三元运算符：(表达式)?成立时返回值:不成立时返回值
		运算符的优先级
	3.3 分支判断
		if结构
		switch结构：
			break是必须的
			case后为常量
		switch与阶梯式if的区别
	3.4 循环
		三种循环
			while循环
			do...while循环
			for循环
		循环嵌套
		两个关键字
			break：结束所有循环
			continue：结束本次循环
	3.5 数组
		数组简介
			数组是一个变量，存储相同数据类型的一组数据
		数组使用方法
			数组的声明
				数据类型[] 数组名 = 分配空间 数据类型[数组长度]
				int[]     ages  =  new int[100];
				ages[0] = 11;
   			数组的赋值
	3.6 数组英语与简单算法
		冒泡排序

